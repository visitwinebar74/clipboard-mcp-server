---
summary: "fix server.json name/identifier and add mcpName for MCP Registry publishing"
breaking: false
security: false
---

# 0.1.3 — 2026-05-25

## Fixed

- **`server.json` name** — corrected from `cyanheads/clipboard-mcp-server` to `io.github.cyanheads/clipboard-mcp-server` to match MCP Registry namespace requirements
- **`server.json` identifier** — corrected from `clipboard-mcp-server` to `@cyanheads/clipboard-mcp-server` (the actual scoped npm package name)
- **`package.json` mcpName** — added `"mcpName": "io.github.cyanheads/clipboard-mcp-server"` required by MCP Registry validation
- **`package.json` publish-mcp script** — added `publish-mcp` script for MCP Registry publishing via `mcp-publisher`
